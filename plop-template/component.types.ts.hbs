export type {{pascalCase name}}Intent = "primary" | "secondary";

export interface {{pascalCase name}}Props {
	children: React.ReactNode;
	intent?: {{pascalCase name}}Intent;
	size?: "small" | "middle" | "large";
	loading?: boolean;
	disabled?: boolean;
	isRounded?: boolean;
	fullWidth?: boolean;
	icon?: React.ReactNode;
}